# Local Setup Guide

1. Clone the repository.
2. Run `docker-compose up -d --build` to start all services (DB, backend, frontend).
3. The frontend is available at `http://localhost:5173`.
4. The backend is available at `http://localhost:8000/docs`.
